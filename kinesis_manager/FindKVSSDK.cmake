if(NOT KVSSDK_EXTERNAL_INCLUDE_DIR)
  set(KVSSDK_EXTERNAL_INCLUDE_DIR /usr/local/src)
endif()

if(NOT KVSSDK_EXTERNAL_LIB_DIR)
  set(KVSSDK_EXTERNAL_LIB_DIR /usr/local/lib)
endif()

find_path(KVSSDK_PRODUCER_INCLUDE_DIR NAMES KinesisVideoProducer.h HINTS ${KVSSDK_EXTERNAL_INCLUDE_DIR})

if(KVSSDK_PRODUCER_INCLUDE_DIR)
  set(KVSSDK_INCLUDE_DIR ${KVSSDK_EXTERNAL_INCLUDE_DIR})
  set(KVSSDK_INCLUDE_DIRS ${KVSSDK_INCLUDE_DIR})
  if(NOT KVSSDK_LIBRARY)
    find_library(KVSSDK_LIBRARY NAMES KinesisVideoProducer libKinesisVideoProducer HINTS ${KVSSDK_EXTERNAL_LIB_DIR})
    if(EXISTS "${KVSSDK_LIBRARY}")
      set(KVSSDK_LIBRARIES ${KVSSDK_LIBRARY})
      set(KVSSDK_FOUND true)
    endif()
  endif()
endif()
